<tr>
  <td><%= link_to test.id, admin_test_path(test) %></td>
  <td><%= test.title %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t(".edit"), edit_admin_test_path(test) %></td>
  <td><%= link_to t(".add_question"), new_admin_test_question_path(test) %></td>
  <td><%= link_to t(".show_questions"), admin_test_path(test) %></td>
  <td><%= link_to t(".delete"), admin_test_path(test), method: :delete, data: {confirm: "Уверены, что хотите удалить этот тест?"} %></td>
</tr>
